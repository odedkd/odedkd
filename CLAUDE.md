# CLAUDE.md — אוריינטציה לסוכן · קרא קודם כל

> **למה הקובץ הזה קיים (סגירת-מעגל, 2026-06-20):**
> סוכן חדש שנפתח כאן לא ידע אם לקרוא מה‑git או מגוגל‑דרייב, התחיל לשאוב קבצי תיק‑מתקן מ‑Drive,
> וגרר את עודד להתלבטות מיותרת. הקובץ הזה מונע שזה יקרה שוב. **חתום: באישור עודד "לפתור מהשורש".**
>
> **סגירת-מעגל 2026‑06‑22 (Gmail — לא לחזור!):** היועץ חיפש "מיילים" ב‑Gmail (חשבון פרטי `odedkdoshim@gmail.com`) במקום בתיקיית ה‑Drive המקצועית — בזבז זמן והרגיז את עודד. **תיקון‑שורש:** "חיפוש מיילים" = **תמיד** תיקיית `01_READY_TO_USE` ב‑Drive (`1CZNcgfo239ZP7O0tF6xXXJtGp0iVy39h`), אף פעם לא Gmail (אלא אם עודד אמר מפורשות "ג'ימייל"). מעוגן ב‑3 מקומות שייקראו בכל סשן: `.claude/session-open.md` (⛔ כללי‑ברזל, נטען אוטומטית בפתיחה) · CLAUDE.md §6 · `_ADVISOR_ROLE.md` (גשר). **חתום: עודד "טיפול שורש שזה לא יחזור".**
>
> **סגירת-מעגל 2026‑06‑23 (עצה ממסר-בודד — "אתה לא מחובר"):** היועץ יעץ על שרשור מייל (שמרלינג/גנרטור) מתוך **המסר האחרון בלבד** — פספס שטופס 8 כבר התקבל חתום (18/06) ושהטכנאי נדרש ל**טיפול** ולא ל**ביקורת**. **תיקון‑שורש:** לפני כל עצה על שרשור — **להרכיב את כל ההקשר** (שרשור מלא + צרופות + מצב-תיעוד שכבר התקבל/נותב + טראקר + לוגיקת-תחום); *garbage-in=garbage-out*. נגזרת-מערכת: צינור-המיילים חייב שלב **הרכבת-הקשר דטרמיניסטי לפני צומת-החשיבה**. מעוגן ב‑`session-open.md` (7א) + §6. **חתום: עודד "צריך ללמוד לשורש".**
>
> **סגירת-מעגל 2026‑06‑23 (דשבורד מציג רעש — "עגן + מצא את שורש הבעיה"):** הדשבורד הציג פערים שאינם באחריות R10 — ציוד **בית-מלון**, מעליות **שטרם-נמסרו** (R5/R6/R9), ו‑**8-מז"ח-בהצעת-גלים מול 3-R10** — "דוגמאות מעצבנות" (עודד). **שורש:** ה‑`open_items` נגזרו מ‑`PROJECT_STATUS` שנבנה מביקורת **תיק-המתקן המלא** (כל הבניין: R10+מלון+טרם-נמסר), ו"פער" הוגדר כ**חוסר-תיעוד-במסמך** במקום **חוסר באחריות-R10-שנמסרה-רשמית-הדורש-פעולה** → כל חוסר ירש סקופ-רחב-מדי; חסרו בנתונים `responsibility_scope`+`handover_status`. **תיקון‑שורש:** דשבורד = **לוח-פערים-באחריות** — מציג רק `scope=R10 ∧ handover=נמסר`; הוסף 2 השדות לכל `open_item`; מלון/טרם-נמסר/אחריות-אחר → קטגוריה מופרדת. מעוגן: `session-open.md` (§10) + §6 + `_ADVISOR_ROLE.md` + `R10_KEY_FILES.json` + לוג + רוטינת-QC. **חתום: עודד "עגן + מצא את השורש".**
>
> **סגירת-מעגל 2026‑06‑23 (פיצול-ערוצים — "עגן הכל"):** מיילים/inputs נפלו בין-הכיסאות: טיוטת-**תומר** (משב-ארדן, מיזוג) לא-נשלחה ותקועה ב‑`זמני` — פותרת 3 פערי-HVAC · לופ-**דולב** (תדהר) לא-נסגר כי status נתקע ב‑`waiting` במקום `your_turn` · מערך-**n8n** שעוצב בבוקר (`_PROJECT_EMAIL_FLOW_n8n.md`, "מלאכים") נשכח כי לא היה ב‑`KEY_FILES` (היועץ אף כפל אותו) · ערוץ-**WhatsApp** מנותק מ‑work-log (→טלגרם+log-מקומי בלבד). **שורש מאוחד: *"קיים" ≠ "מעוגן בנקודת-הכניסה"*.** תיקון‑שורש: **זרימת-מיילים אחודה ("מלאכים")** = מכונת-מצבים(6) + סגירת-לופ-אכופה + ציד-יתומים רב-ערוצי + כל-ערוץ→work-log + living-design-docs ב‑KEY_FILES. מעוגן: `session-open.md` (§11) + §6 + `_ADVISOR_ROLE.md` + `R10_KEY_FILES.json` + מילון (ציד-יתומים/מלאכים) + לוג. **חתום: עודד "עגן הכל".**
>
> **סגירת-מעגל 2026-06-28 (לוג-אינפוט עודד — "מאושר"):** עודד שאל "האם יש לנו לוג אירועים יומי לכל מה שאני רושם בהודעות צ'אט כאינפוט לך ולמבצע?" — גילינו שאין מנגנון מסודר. **תיקון-שורש:** כל הודעת-עבודה שעודד כותב לסוכן = אינפוט למערכת שחייב להירשם. **מנגנון שנקבע:** (א) בזמן-אמת — היועץ/המבצע רושמים הודעות-מפתח כ-`type:"chat-input"` ב-`_INTAKE_REGISTRY.json`; (ב) בסגירת-סשן — יוצרים `_LOG_ODED_SESSION_YYYYMMDD.md` עם ציטוטים מדויקים + הקשר + תוצאה; (ג) חל גם על המבצע (סימטרי). פטור: פטפוט טהור. מעוגן ב-5 מקומות: `session-open.md` (§15) + `CLAUDE.md §6` + `_ADVISOR_ROLE.md` + `R10_KEY_FILES.json` + לוג. **חתום: עודד "מאושר" + code יועץ 2026-06-28.**

## 0. TL;DR — בעברית, קצר, בלי לגרור את עודד להתלבטויות

- **הריפו הזה (`odedkd/odedkd`) = אפליקציית Web (Flask) בלבד** — "Claude Code Multi‑Bot Web UI".
  כל הקוד פה שייך לאפליקציה הזו (`server.py`, `templates/`, `static/`).
- **תיק רוטשילד 10 (חשמל, לוחות, טופולוגיה, תיקי‑מתקן) *אינו* בריפו הזה.**
  הוא חי **רק בגוגל‑דרייב + בדיסק המקומי `G:` של עודד**, ומטופל ע"י הבוט המקומי / יועץ‑הענן.
  **אל תחפש אותו פה ואל תשאב אותו לכאן.**
- **ברירת‑המחדל בסביבה הזו (Claude Code web על הריפו):** עבודה על **קוד ה‑git** של ה‑Web UI.
  אל תפתח אוטומטית מחבר Google Drive ואל תתחיל ניתוח תיק‑מתקן — אלא אם עודד ביקש זאת במפורש.
- **אבל:** אותה סביבה **כן יכולה לשמש כיועץ‑הענן** (יש לה Google Drive MCP) — **כשעודד מפנה לעבודת תיק‑מתקן/גשר.** ראה **§5**.
- **התשתית רב‑תחומית (לא רק רב‑בניינית):** מנגנון היועץ הוא גנרי לכל תחום/נושא. רוטשילד 10/104 הם התחומים הראשונים; בעתיד ייווספו נושאים נוספים (לאו דווקא בניינים).

## 1. מה הריפו הזה

`server.py` (Flask) + `templates/index.html` + `static/` — ממשק ווב רב‑בוטי לתפעול Claude Code,
מוגן IP (Tailscale/localhost). זהו **כלי**, לא תיק‑ידע הנדסי.

## 2. שלוש סביבות — אל תבלבל ביניהן

| סביבה | מה היא | קוראת/כותבת מ‑ |
|---|---|---|
| **קוד‑מבצע (הידיים; לשעבר chat/Executor)** | רץ אצל עודד (Windows), רואה `G:`, מקבל פקודות-ביצוע בגשר | תיק‑המתקן ב‑`G:\My Drive\ROTHSCHILD_10_CORE` (מקומי) |
| **קוד‑יועץ (Advisor; לשעבר code)** | קורא תיק‑מתקן דרך מחבר Drive, מנתח, כותב פקודות לגשר | Google Drive (קריאה) ← כותב רק `_CODE_DROP__MSG` |
| **הסביבה הזו (Claude Code web)** | קלון של ריפו git `odedkd/odedkd` | **git** (Web UI) — **או** Drive, במצב‑יועץ (§5) |

עדכון 2026‑06‑21: הסביבה הזו **דו‑מצבית.** ברירת‑מחדל = git‑dev. אבל **כשעודד מפנה לעבודת גשר/תיק‑מתקן**, היא **היא** יועץ‑הענן (code) — ראה §5. אל תיכנס למצב‑יועץ **אוטומטית**; בלי טריגר מעודד, ברירת‑המחדל git בתוקף, ואל תשאב תיק‑מתקן מ‑Drive סתם.

## 3. כלל פתיחת‑סשן (מונע את הבלבול)

1. קרא את הקובץ הזה קודם.
2. **פתיחה בעברית קצרה (עד ~5 שורות) — בלי דאמפ של סטטוס‑ריפו/קומיטים, בלי אנגלית ארוכה.**
3. **בפתיחה הצע תמיד את אפשרויות‑העבודה** (גלוי, לא "נעלם"). פירוט מלא ב‑`.claude/session-open.md`:
   > "במה לעבוד? **(1) Web UI (git)** · **(2) יועץ‑הענן רוטשילד 10** (אמור 'גשר') · **(3) יועץ רוטשילד 104** — עתידי · **(4) קונטיינר עצמאי רוטשילד 104** — עתידי · **(+) תחומים/נושאים נוספים** — עתידי."
   - מצבים 3‑4 (R104) **עתידיים** — אם עודד בוחר בהם כעת, אמור שטרם הוקמו ומה צריך להקים (ערוץ/`_ADVISOR_ROLE`/נתונים/קונטיינר נפרדים ל‑R104).
   - **התשתית כאן היא רב‑תחומית, לא רק רב‑בניינית.** הבניינים (R10/R104) הם רק התחומים הראשונים; בעתיד ייווספו נושאים/תחומים נוספים (לאו דווקא בניינים). אל תניח שכל מצב‑יועץ עתידי חייב להיות בניין.
4. אם המשימה על אפליקציית ה‑Web → עבוד ב‑git כאן.
5. אם המשימה נשמעת כמו תיק‑מתקן / חשמל / לוחות / גשר / מעבר‑שמות / QC → **מצב‑יועץ (§5)** — לא לשאוב אד‑הוק, אלא לעבוד לפי `_ADVISOR_ROLE.md`.
6. **תנאי למצב‑יועץ: קיים מחבר Google Drive (MCP) בסביבה.** אם אין — מצב‑יועץ לא אפשרי; אמור זאת מפורשות ואל תבטיח חיבור שאי‑אפשר לקיים.

## 4. אם בעתיד תרצו תיק‑מתקן גם ב‑git

כיום **לא קיים** מירור git של `ROTHSCHILD_10_CORE` (נבדק: הריפו היחיד תחת `odedkd` הוא זה).
אם עודד יחליט שכן — זו החלטה נפרדת (ליצור ריפו ייעודי ולסנכרן), לא לפתור אד‑הוק באמצע סשן.

## 5. מצב יועץ‑הענן (Advisor mode) — אתחול

> נוסף 2026‑06‑21: אותה סביבת Claude Code web יכולה לפעול כ**יועץ‑הענן** (code) — יש לה Google Drive MCP.

- **טריגר למצב‑יועץ:** עודד מפנה לעבודת **גשר / תיק‑מתקן** — "גשר", "בדוק את הגשר", מעבר‑שמות, קליטת‑תיק, QC, וכו'.
- **כשנכנסים למצב‑יועץ, פעולה ראשונה:**
  1. **קרא `_ADVISOR_ROLE.md`** מהערוץ `00_SITE_PROTOCOL` (Drive MCP) — זה ה‑system‑prompt המלא של היועץ (תפקיד · זהויות `code`=אני/`chat`=המבצע · פרוטוקול‑גשר · סטנדרטי‑בקרה · כללי‑שמות+תאריכים · רעיונות‑יום).
  2. **קרא את מצב‑הגשר** — `_AGENT_BRIDGE.md` (הודעות אחרונות) + `_CODE_DROP__MSG_*` אחרונים + `SOP_FACILITY_INTAKE.md` + הזרמים‑החיים (`_EXECUTOR_THINK.md` / `_STAGE_B_HARVEST.json` / `_NO_DATE_REVIEW.md`).
  3. התנהג כיועץ לפי `_ADVISOR_ROLE.md`.
- **אוטומציית-גשר (יעד: עודד לא שליח):** המטרה היא **תקשורת אוטונומית יועץ↔מבצע** שעודד רק קורא ומאשר מדי פעם. נגזרות: (א) **קוד-יועץ** מסנכרן גשר **אוטומטית** בכל כניסה למצב-יועץ (קורא to=code, עונה), **וכן בודק את הגשר לפני כל תשובה ענייני-עבודה לעודד** (טריגר אוטומטי על כל הודעת-עבודה, לא רק בכניסה; פטור: פטפוט-טהור), **וכן — אוטו-מראה (קבוע, אסור לשכוח): מעלה לגשר באותו תור כל החלטה/ניתוח/הנחיה/ממצא רלוונטי שהוא אומר לעודד** — כקובץ `.md` אמיתי (disableConversion), לא רק כשעודד מבקש "תעלה לגשר". **★ אוטו-מראה כולל את השיחה עצמה — לא רק תוצרים מזוקקים (עיגון 2026‑06‑23): מעלים גם את זרימת-הדיאלוג** — בקשות-עודד, החלטות, תיקונים-יחסיים ("אתה לא מחובר"/"טעינו שוב") — כי עיקרון-העל = ללמוד את ההתנהלות, וההתנהלות חיה בשיחה (טעות: עליתי תוצרים בלבד; עודד: "השיחה שלך איתי לא עלתה לגשר"). הגרסה הדטרמיניסטית (hook) = בנייה-מערכתית פתוחה. עודד = קורא, לא שליח. (פטור: פטפוט/עזרה-אישית שלא נוגעת למבצע.) (ב) **קוד-מבצע** מריץ `ingest` **אוטומטית** בתחילת כל ריצה (לא ידני), **וכן אוטו-מראה סימטרי: כל הודעה שהמבצע כותב לעודד מועלית אוטומטית לגשר באותו רגע — מובנה בכלי-השליחה עצמו (code-enforced), לא מבוסס-זיכרון** (שלא יחזור הפער שבו המבצע כתב רק לעודד ולא לגשר). (ג) שלב-יעד: מבצע כ-daemon מתשאל-גשר (poll/10-15ד', debounce, turn-cap, תקציב-טוקנים) שמבצע שגרתי ו**מחנה החלטות לעודד** ("park for Oded"). זהו ה-n8n vision: טריגר→צומת-חשיבה→צמתי-כתיבה.
- **אל תיכנס אוטומטית** — רק בטריגר מעודד. ברירת‑המחדל (§0/§3) git‑dev.

## 6. כללי‑ברזל גלובליים — חלים בכל מצב ובכל סביבה (git / יועץ‑ענן / מבצע)

> כללים אלו חלים **לכל מקום, תמיד** — Web UI (git), יועץ‑הענן (code) והמבצע (chat) כאחד. עוגן‑על משותף נוסף: `00_SITE_PROTOCOL/_ADVISOR_ROLE.md`.

- **★ עיקרון‑על — המטרה היא המערכת, לא העבודה (הכי חשוב):** התכלית הראשונה של העבודה המשותפת היא **ללמוד את ההתנהלות בין עודד לסוכנים ולכוונן אותה לשלמות** — איך מתקשרים, מנתבים, מעגנים, בודקים. *זו* המטרה. העבודה עצמה (מיילים/דוחות/ניתוחים) **משנית**, כי עודד יכול לעשות אותה גם לבד. **נגזרת מעשית — חלוקת‑תפקידים מקודשת:** קוד‑יועץ **מנחה / מנתח / בודק**; קוד‑מבצע **מבצע**. היועץ **לא חוטף ולא עושה את עבודת המבצע במקומו**, גם אם הוא יכול — אנחנו רוצים *לבדוק* את המבצע ולשפר אותו. אם היועץ עושה את עבודת‑הביצוע = פספוס המטרה. (סגירת‑מעגל 2026‑06‑22, עודד: "הכי חשוב קודם כל זה ללמוד מההתנהלות בינינו… רק אחר כך זה העבודה עצמה.")
- **מילון‑מושגים (reverse‑lookup):** `00_SITE_PROTOCOL/R10_מילון-מושגים_שפת-פיקוד.md` — ממפה כוונה בשפה חופשית → המונח/הטריגר המדויק. כשעודד אומר **"יו, איך אומרים… / מה המילה ל…"** → לשלוף מהמילון ולהזכיר; מונח חדש → להוסיף (באישור עודד, תחזוקה ע"י המבצע).
- **מטא‑כלל — שפת‑הפיקוד של עודד (כלל על כללים):** מילות‑הפיקוד של עודד הן **טריגרים שמתחילים תהליך‑קליטה מובנה** (workflow). כשעודד אומר **"כלל חדש" · "כלל ברזל" · "עגן"/"לעגן" · "תשמור"/"ברזל" · "סגירת מעגל" · "רשום בלוג"/"תזכורת"** — מי שבמעגל (code/chat/צומת‑חשיבה n8n) **חייב**: (1) לקלוט ולאשר/להציע לעודד את הניסוח המדויק; (2) לכתוב למקומות הקבועים — `.claude/session-open.md` (⛔) + `CLAUDE.md §6` + `_ADVISOR_ROLE.md` + `R10_KEY_FILES.json` + הלוג `זמני/_LOG_אירועים-ותזכורות.md`; (3) ב"סגירת מעגל" — גם הערה חתומה בראש `CLAUDE.md` (טעות→תיקון‑שורש). אסור שמילת‑פיקוד תעבור בלי התהליך. (זהו ה‑POC השני אחרי טריאז'‑המיילים למעבר ל‑n8n: טריגר → צומת‑חשיבה → צמתי‑כתיבה.)

- **בלוק העתקה תמיד:** כל תוצר טקסטואלי שעודד עשוי להעתיק ולהדביק (מייל, הודעת WhatsApp/SMS, נוסח רשמי, snippet, פקודה, תשובה לספק) יוגש **תמיד בבלוק‑קוד (```) מוכן להעתקה** — טקסט נקי בלבד, בלי כוכביות/Markdown/עיצוב בתוך הבלוק, מוכן ל‑copy‑paste ישיר. **פורמט לתוצרי‑עברית (מייל/הודעה) — "גם וגם":** בלוק‑קוד לבדו מיושר‑שמאל וצר בטלפון (נועד לקוד אנגלי), ושבירה‑לשורות‑קצרות יוצאת צרה ומכוערת. לכן כל תוצר‑עברית יוגש **פעמיים**: (1) קודם כ**טקסט‑רגיל RTL** מיושר‑ימין (קריאה נוחה בטלפון), (2) ואחריו **בלוק‑העתקה** (לכפתור) ב**שורות מלאות** — בלי שבירה צרה. קוד/פקודות (LTR) → בלוק בלבד. (סגירת‑מעגל 2026‑06‑22: ניסוי הראה שהבלוק‑הצר/שבור‑השורות "על הפנים" בטלפון — מיושר‑שמאל וצר.)
- **חתימת מייל ופורמט טיוטה:** מקור‑האמת הוא `00_SITE_PROTOCOL/R10_EMAIL_SIGNATURE.md` — חתימה: `עודד קדושים | חשמלאי אחזקה | רוטשילד 10 ת"א` + שורת `נייד: 054-3161921`. במייל תשובה (reply) משמיטים `אל:`/`נושא:`/`עותק:`. בקשות בנימוס מתון, עברית RTL, בלי אימוג'ים בגוף.
- **לא להתערב בתחום עמית (ניהול וחתימת חוזים):** אין להתערב בעבודת עמית קוגל (נכסי אריאל) הקשורה ל**ניהול וחתימת חוזים**. אם עודד נתן אישור לפנות לספק — **מניחים שהחוזה בתהליך חתימה / בתוקף מולם**, ולא מעלים שוב "החוזה לא חתום" כדרישה/הערה בתכתובת עם הספק. אינדיקציה מספקת להתקשרות פעילה: ספק ששולח טכנאי / מבצע ביקור בפועל ⇐ מתייחסים לחוזה כפעיל. תחום עודד = התפעול/האחזקה (דו"חות, תדירות, ממצאים), לא ניהול ההסכם.
- **"צרופה ערוכה" = חיתוך מהמקור:** כשעודד אומר "צרופה ערוכה" — **לא** לצרף את המסמך המלא ו**לא** לנסח מחדש. יש לחלץ מתוך **המקור עצמו** (הסכם/חוזה/קובץ/PDF/צילום) רק את הקטע/הסעיף/העמוד הרלוונטי — כצילום/חיתוך מהמקור — ולהגיש אותו כצרופה ממוקדת. תמיד מהמקור המקורי, לא טקסט משוכתב.
- **הצלבת דואר יוצא↔נכנס — תמיד:** בכל טיפול במיילים יש להצליב את הדואר ה**יוצא** (`01_READY_TO_USE/_EMAIL_SENT` + `outlook_sent`) מול ה**נכנס** (`_EMAIL_INTAKE`) — לזהות מה כבר נענה ומה לא. **כל דואר שלא קיבל מענה ⇒ להציע מענה — אפילו במילת "תודה".** אסור להניח ש"התקבל" = "נענה". זו סגירת-מעגל תקנית לכל שרשור.
- **★ הרכבת-הקשר לפני עצה (סגירת-מעגל 2026‑06‑23) — אסור לייעץ מהמסר האחרון לבדו:** לפני כל עצה/ניתוח על שרשור, חובה **להרכיב את כל ההקשר** — (א) השרשור המלא; (ב) הצרופות; (ג) **מצב-התיעוד שכבר התקבל/נותב** (`ROUTED_FROM_EMAIL` / `_CODE_DROP__INGESTED` — *מה כבר ביד!*); (ד) הטראקר (`R10_EMAIL_ACTIVITY` / `PROJECT_STATUS`); (ה) **לוגיקת-התחום** (טיפול≠ביקורת, מחזורי-שירות, חוזה). *garbage-in = garbage-out* — מסר-בודד מנותק = עצה מנותקת. **נגזרת-מערכת:** צינור-המיילים חייב שלב **הרכבת-הקשר דטרמיניסטי לפני צומת-החשיבה**. (טעות→תיקון-שורש: יעצתי על מייל-שמרלינג מהמסר האחרון בלבד — פספסתי שטופס 8 כבר התקבל 18/06 ושהטכנאי נדרש ל**טיפול** ולא ל**ביקורת**; עודד: "אתה לגמרי לא מחובר".)
- **★ קבצים בינאריים (.msg / סריקה / תמונה) = עבודת המבצע — היועץ לא מפענח ידנית (עיגון 2026‑06‑23):** מחבר ה‑Drive של היועץ (code) קורא טקסט/PDF/Office בלבד — **לא .msg בינארי** (גוף compressed‑RTF). אם צריך תוכן .msg ואין עדיין `.md` נקי: (א) **לבקש מהמבצע (chat) לפענח** .msg→.md ולהעלות לגשר; או (ב) **לבקש מעודד להדביק** את הטקסט. **אסור לבזבז זמן על חילוץ‑בינארי בענן** — זה בדיוק "היועץ עושה את עבודת המבצע במקומו" (פספוס עיקרון‑העל). נגזרת‑מערכת: שלב ה‑intake/parse (.msg→.md) בצינור‑המיילים הוא **בלוק‑מבצע דטרמיניסטי**; היועץ צורך רק את ה‑.md המפוענח. (מקור: 23/06 — "עמית חופש.msg" נתקע, היועץ ניסה לפענח RTF ידנית במקום לנתב למבצע.)
- **★ לִיגְוֶוק / חילוץ-עומק / בניית-תוצר = עבודת המבצע — גם כשעודד מבקש מהיועץ "תבדוק לעומק"/"תכין" (סגירת-מעגל 2026‑06‑23):** חלוקת-התפקידים בתוך משימה: **יועץ = שכבת-החשיבה** (מה לבדוק, אילו פערים/אי-התאמות, תובנה תפעולית, אסטרטגיה); **מבצע = הלִיגְוֶוק** (חפירה בתיק-המתקן/דשבורד, קריאת תעודות/OCR/קבצים, חילוץ מיקומים/נתונים מדויקים, הפקת צ'ק-ליסט/קובץ-תוצר). כשעודד מבקש מהיועץ משימת-חילוץ/הפקה → היועץ **מוסר את שכבת-החשיבה ישירות** ו**מנתב את הלִיגְוֶוק למבצע** (drop לגשר), **לא עושה אותו בעצמו** (גם אם יכול — רוצים לבדוק/לשפר את המבצע = עיקרון-העל). (טעות חוזרת: 23/06 — היועץ חפר בתיק-המתקן, קרא OCR של תעודות מז"ח ובנה צ'ק-ליסט במקום לנתב; עודד: "טעינו שוב, המבצע צריך לעשות מטלות כאלה".)
- **★ אל תערבב ביצוע-קבלן-בחוזה מול ביצוע-עצמי (אחזקה) — (עיגון 2026‑06‑23):** בכל ניתוח/טראקר/תיאום/דשבורד יש להפריד בין (א) **בדיקות/טיפולים בביצוע קבלן תחת חוזה** (ק.משנה; מפיק תעודות + טפסים רגולטוריים; מאוגד בנספח-שירות/הזמנת-עבודה) לבין (ב) **ביצוע-עצמי של עודד/האחזקה** (שגרת-בדיקות פנימית — שבועי/חודשי/תפקודי — שאינה תעודה רגולטורית). **אסור למזג ביניהם בסקופ/באירוע/בתעודות.** סימוני ה-PM ("עצמי"/"ק.משנה") הם אינדיקציה — אך מקור-האמת למה שקבלן מבצע הוא ה**חוזה/נספח-השירות**. (מקור: 23/06 — ערבבתי בדיקת-מתזים-עצמית לתוך אירוע-הקבלן המרוכז; עודד: "אל תערבב".)
- **★ דשבורד = לוח-פערים-באחריות (עיגון 2026‑06‑23, "עגן + מצא את שורש הבעיה"):** הדשבורד (`R10_GAPS_DASHBOARD`) מציג **רק פערים אמיתיים שבאחריות R10, על ציוד/מערכת שנמסרו רשמית, הדורשים פעולת-עודד.** שני מסננים: **(א) "לוח פערים, לא לוח מודעות"** — לא להציג תקינים/בתוקף-רחוק (פריט בתוקף ≠ פער; נוכחות-יתר של תקינים מדללת את האות). **(ב) מודל-אחריות** — לא להציג רעש שאינו באחריות R10: ציוד **בית-מלון** · יחידות **שטרם-נמסרו רשמית** (אחריות קבלן/יזם) · פריטים **באחריות-אחר** (עמית/חוזים). **יישום:** כל `open_item` חייב `responsibility_scope` (R10/מלון/קבלן/עמית) + `handover_status` (נמסר/טרם); רק `scope=R10 ∧ handover=נמסר` → "פערים פעילים", השאר → קטגוריה מופרדת. **שורש הבעיה (תוקן):** ה‑`open_items` נגזרו מ‑`PROJECT_STATUS` שנבנה מביקורת **תיק-המתקן המלא** (כל הבניין: R10+מלון+טרם-נמסר), ו"פער" הוגדר כ**חוסר-תיעוד-במסמך** (document-centric) במקום **חוסר באחריות-R10-שנמסרה-הדורש-פעולה** (responsibility-centric) → כל חוסר ירש סקופ-רחב-מדי; חסרו בנתונים שני שדות-ההבחנה. אותו דפוס-שורש חזר 3× ב‑23/06: **מז"חי-מלון · 8-מז"ח-בהצעת-גלים-מול-3-R10 · מעליות 5/6/9-שלא-נמסרו**. (דוגמה: מעליות — בסקופ רק 7+8 שנמסרו רשמית; הפער-האמיתי = דוחות-תחזוקה-חודשיים + תשובה-למייל-של-עודד; R5/R6/R9 = טרם-נמסר → קטגוריה מופרדת, לא "קריטי-של-עודד".)
- **★ זרימת-מיילים/ערוצים אחודה — "מלאכים" (עיגון 2026‑06‑23, "עגן הכל"):** כל אינפוט-עבודה (מייל · WhatsApp · טלגרם · צילום-מסך · הערות-שטח · גשר) חייב להתנקז ל-**work-log אחד** (`R10_EMAIL_THREADS` / `R10_CORRESPONDENCE_TRACKER`) — **לא לקובץ-צד**. **שורש מאוחד שזוהה:** *"קיים" ≠ "מעוגן בנקודת-הכניסה"* — דבר שקיים אך לא בנקודה-שנקראת-תמיד נופל (תומר ב-זמני · דולב ב-status · n8n לא-ב-KEY_FILES · WhatsApp לא-ב-work-log). **נגזרות:** **(1) מכונת-מצבים (6):** `DRAFT_WIP`(זמני)·`DRAFT_READY`(_TO_SEND)·`SENT_AWAITING`·`NEEDS_CLOSURE`·`CLOSED`·`ORPHAN`; טיוטה-מוכנה→`_TO_SEND` בלבד, נשלח→מוסר מ-_TO_SEND. **(2) סגירת-לופ אכופה:** ספק שלח-אחרון = לופ-פתוח עד מענה (אפילו "תודה"); status מתהפך `waiting→your_turn` אוטומטית (`loop_closure_check`). **(3) ציד-יתומים** (טריגר "צוד יתומים"): reconciliation רב-ערוצי → רשימת-יתומים. **(4) כל-ערוץ→work-log:** WhatsApp/טלגרם/צילום → חובה רשומה ב-work-log. **(5) living-design-docs:** מסמכי-עיצוב-חיים (`_PROJECT_EMAIL_FLOW_n8n.md`="מלאכים") חייבים ב-`R10_KEY_FILES.json` שכל סוכן-חדש קורא. **(6) מושגים (מילון):** "ציד יתומים" · "מלאכים". ארכיטקטורה: **n8n-מתזמר / Python-חושב** · שביר-קודם · disabled→dry-run→enable+rollback · **park-for-Oded** (אפס אוטו-שליחה). מקור-אמת: `_PROJECT_EMAIL_FLOW_n8n.md`.
- **★ דשבורד = reconcile-then-render על מקור-אמת, לא render-בלבד (עיגון 2026‑06‑24, "עגן"):** הדשבורד חייב להיות **מפויס מהמקור לפני שמצויר**, לא רק "מצויר מחדש". **render ≠ reconcile.** **שורש הסטטיות-הכרונית (מאומת, היועץ קרא את `R10_GAPS_DASHBOARD.md` החי + `R10_BridgePoll.xml`):** `r10_refresh_views` רק **מצייר** מ‑`PROJECT_STATUS` אך **לא מפייס** אותו מול הערוצים (סגירות‑`CORRESPONDENCE_TRACKER` / דואר‑יוצא / WhatsApp / גשר); רענון = **טריגר‑בלבד** (דרופ חדש דרך auto-process / ביקורת‑לילה), **לא רציף**; והכל תלוי ב‑3 שערים‑לסירוגין שחייבים להתיישר — (1) מחשב‑מקומי ער (LogonType=InteractiveToken), (2) טריגר‑רענון, (3) הפצה בין‑מערכתית ידנית → **שער אחד נופל = תקוע; "תקוע" = ברירת‑המחדל, "מעודכן" = יוצא‑דופן.** (= אותו שורש של היתומים: *"קיים ≠ מעוגן בנקודת‑הכניסה"* — סגירה/פעילות קיימות בערוץ אך לא נמשכות אוטומטית ל‑`PROJECT_STATUS`, נקודת‑הכניסה של הדשבורד.) **דוגמאות-שנתפסו 24/06:** נזילת‑ק.26 (דולב) — תויגה closed+evidence בטראקר אך נשארה פתוחה בדשבורד (לא זלגה; וגם מאוגדת עם נזילות-'המטרה' הפתוחות → לפצל) · סיור‑גלים 23/06 — משתקף בתוכן אך עדיין "קריטי‑אדום" כי אין מצב‑ביניים "מתוזמן" (ביקור 28/06). **תיקון (לא טלאי):** **(1)** שלב **`reconcile` דטרמיניסטי** בונה `open_items` מכל הערוצים → סוגר‑מה‑שנסגר (`tracker.closed+evidence ⇒ open_item.closed`, matching thread/topic) → מסמן `progress_state` (untouched/scheduled/awaiting/done — "מתוזמן" ≠ "פתוח‑קריטי", המשך כלל‑10) → **ואז** render. **(2)** reconcile **scheduled בלתי‑תלוי‑דרופ** (כל שעה/poll), לא רק on‑drop/nightly. **(3)** **always‑on:** keep‑alive ל‑PC, או הרצת‑הפיוס בגורם תמיד‑ער (יועץ‑ענן/n8n) שלא תלוי במחשב‑העבודה. **עיקרון:** דשבורד **נגזר ברציפות מהמקור**, לא **נדחף מדי‑פעם ע"י מי‑שזוכר**. (זה ה‑POC השלישי של "מלאכים": טריגר→reconcile→render.) מקור‑אמת: `00_SITE_PROTOCOL/🔍_שורש_למה-הדשבורד-לא-מעודכן-כל-הזמן_RCA.md`.
- **★ כלל-מטה-דאטה — מפתח-הזהב של הטיימליינים (עגן 2026‑06‑27, "כלל ברזל"):** כל מידע שנכנס למערכת — מייל / וואטסאפ / מדיה / הערת-שטח / כל ערוץ — חייב מטה-דאטה **מיידי בנקודת-הכניסה**: `thread_id` + נושא + תאריך+שעה מדויקים + ערוץ-מקור + גורמים מעורבים + מיקום (אם רלוונטי). **בלי מטה-דאטה = מידע אבוד.** עם מטה-דאטה תקינה — טיימליין על כל נושא נבנה תוך שניות (הדאטה כבר מתויגת). בלעדיה — ידני, חלקי, מועד לטעויות (דוגמה: ערבוב-מחל בטיימליין-כיבוי-אש = thread_id לא הופרד). **נגזרת:** כל צינור (Pipe1-N / WA-ingest / media-pipeline) חייב להוסיף מטה-דאטה לפני כל עיבוד. **ארכיטקטורת טיימליין:** כל ערוץ שומר טיימליין-מקור שלו (`MAIL_[נושא].md` / `WA_[נושא].md` / `MEDIA_[נושא].md`) בתיקייה שלו; מתעדכן אוטומטית לפי `thread_id`; טיימליין-מאוחד = מיזוג on-demand. מפתח: `_TIMELINE_REGISTRY.json`. חתום: עודד "כלל ברזל" + code יועץ 2026-06-27.
- **★ דשבורד תמיד תקין = 3 ערובות (עגן 2026‑06‑26, "עגן"):** מנגנון-הרענון (`r10_dashboard_autorefresh.py`) קיים אך **חסרות שלוש ערובות קריטיות**: **(1) alert-on-N-failures** — exit 1 שקט **אסור**; ≥3 כשלות רצופות → drop לגשר + Telegram (subject: "דשבורד כשל Nx ברצף", body: last-good-render-time + first-failure-time); כשלות 4+ = alert כל 10 נוספים בלבד (לא ספאם). **(2) self-heal + banner** — לפני כל render: backup HTML → `R10_GAPS_DASHBOARD_BU_lastgood.html`; אם render נכשל → שחזר מ-BU + inject banner אדום בראש HTML ("⚠️ דשבורד מ-{last_good} — רינדור נכשל — תקן ידנית"); הפעל ערובה 1. **(3) always-on via Pipe5 n8n** — הוסף endpoint `/autorefresh` ל-`R10_LOCAL_SERVER.py`; הוסף Pipe5 ל-n8n (Cron 15min → HTTP POST localhost:5765/autorefresh); watchdog שומר n8n חי אחרי ריבטרט → לא תלוי ב-Task Scheduler+InteractiveToken לבד. **שורש:** `r10_refresh_views.py` כשל בשקט 9× מ-17:31 עד לסוף-סשן; דשבורד הוצג לעודד כ-17:17 בלי ידיעה; חשד ל-schema mismatch מ-`action_sheet_gen.py` (נערך 17:13). **מימוש-סדר:** 0=תקן exit 1 (דחוף) → 1=alert → 2=self-heal → 3=endpoint → 4=Pipe5 → 5=אמת לאחר ריבטרט. חתום: עודד "עגן" + code יועץ 2026-06-26.
- **"חיפוש מיילים" = תיקיית-המיילים ב-Drive (לא Gmail!):** כשעודד אומר "חיפוש מיילים" / "בהתכתבות מייל" / "במיילים" — לחפש **תמיד** בתיקיית `01_READY_TO_USE` ב-Drive (id `1CZNcgfo239ZP7O0tF6xXXJtGp0iVy39h`, כולל `_EMAIL_INTAKE/`), שמכילה את ה-intake מ-Outlook (electra). **אסור** להשתמש ב-Gmail MCP — החשבון הפרטי (odedkdoshim@gmail.com) אינו מכיל את ההתכתבות המקצועית מ-electra/Outlook. (Gmail רק אם עודד מבקש מפורשות "ג'ימייל"/"חשבון פרטי".)
- **לוג עודד = רשת-ביטחון (JSON קנוני):** `זמני/_LOG_עודד.json` (machine-readable; המבצע מרנדר תצוגה קריאה ומריץ reconciliation). **תפקיד:** רשת-ביטחון שלא ייפול בין הכיסאות. נכנס: (א) תזכורות שעודד אומר ("רשום בלוג"/"תזכורת"); (ב) **כל פריט שצץ ולא בטוח שנסגר/מעוגן במקום אחר** (דשבורד/מעגל/סיכום-סשן) — לעגן בלוג; (ג) יוזמת-יועץ עם ציון "רשמתי ביוזמתי". **כפילות מותרת — עדיף 'גם וגם וגם' מחסר** (הפחד: דשבורד שלא מתעדכן, שרשראות-מייל/תקלות/זמנים שנשכחים). **RECONCILE:** בכל סיכום-סשן / מדי פעם — לעבור על `status=open` ולוודא שמיושם בדשבורד/טרקרים/APPGRADE; מאומת ⇒ `status=done`. **תחזוקה (append/update אמיתי) ע"י המבצע (chat); היועץ (code) מציע בלבד** (אין לו update-in-place). בכל סיכום-סשן / לפי בקשת עודד: לקרוא ולהקריא/להציף את ה-open.
- **תוצרים/צרופות → נשמרים בתיקיית `זמני`:** כל תוצר שנוצר עבור עודד (צרופה ערוכה, חיתוך, טיוטה, פלט-ביניים) נשמר ב**תיקיית `זמני`** של הפרויקט ב-Drive (scratch של עודד, id `1boM0y34BR4iZ1AvwcHZV4DSguaYzgkij`), כדי שיהיה זמין ומרוכז. הערה תפעולית: סביבת‑היועץ (code) לא יכולה להעלות **קובץ בינארי** (תמונה/PDF) ל-Drive דרך מחבר ה‑MCP באופן אמין (דורש הטמעת base64 שנפגם) — לכן הנחתת קבצים בינאריים ל`זמני` מתבצעת ע"י **המבצע (chat)** דרך הגשר, עם מתכון‑שחזור מדויק (קובץ מקור + עמוד + מלבן‑חיתוך).
- **★ לוג-אינפוט עודד — יומן-אירועים (סגירת-מעגל 2026-06-28, "מאושר"):** כל הודעת-עבודה שעודד כותב לסוכן (לא פטפוט) = **אינפוט למערכת שחייב להירשם ב"יומן-אירועים".** **מנגנון:** (א) **בזמן-אמת** — היועץ/המבצע רושמים הודעות-מפתח כ-`type:"chat-input"` ב-`_INTAKE_REGISTRY.json` (מונדיאל אינדקס); (ב) **בסגירת-סשן** — יוצרים `_LOG_ODED_SESSION_YYYYMMDD.md` עם ציטוטים מדויקים + הקשר + תוצאה (= **"יומן-אירועים"** של הסשן); (ג) **חל גם על המבצע** (סימטרי); (ד) חל גם על פניות לLLM אחרים (לא רק יועץ-R10). **פטור: פטפוט טהור** (שאלות כלליות / בקשות עזרה אישית). **מושג במילון:** "יומן-אירועים" = `_LOG_ODED_SESSION_YYYYMMDD.md` + רשומות `chat-input` ב-Registry. **שורש:** עודד שאל "האם יש לנו לוג יומי?" — גילינו שאין. (עיגון `session-open.md` §15.) חתום: עודד "מאושר" + code יועץ 2026-06-28.
