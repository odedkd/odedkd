---
name: ask
description: >-
  מענה משאלות-עבר על תיק-המתקן רוטשילד 10 — קורא את _QA_CACHE_v2.json (מטמון
  שו"ת) ועונה ממנו לפני חיפוש מלא בתיק. הפעל כשעודד שואל שאלה עובדתית על
  המתקן: גילוי-אש / רכזת / זונים / התקנים, לוחות חשמל, טופולוגיה, נהלים, QC.
  עובד גם מקומית (מבצע Windows, דיסק G:/D:) וגם במצב יועץ-ענן (Google Drive MCP).
---

# /ask — מענה ממטמון-השו"ת של רוטשילד 10

**עיקרון-על: תשובה ישירה ומדויקת קודם. אם הנתון קיים במטמון — בלי שאלות-נגד ובלי חיפוש מיותר.**

הקלט: השאלה של עודד (ב-args או בהודעה). אם אין שאלה — שאל "מה השאלה?" ועצור.

## שלב 1 · אתר את המטמון (נתיב כפול)

בדוק לפי הסדר ועצור בראשון שקיים:

| # | סביבה | נתיב |
|---|---|---|
| 1 | מקומי — G: | `G:\My Drive\ROTHSCHILD_10_CORE\00_SITE_PROTOCOL\_QA_CACHE_v2.json` |
| 2 | מקומי — מירור D: | `D:\my drive\ROTHSCHILD_10_CORE\00_SITE_PROTOCOL\_QA_CACHE_v2.json` |
| 3 | ענן — Drive MCP | קריאה לפי file-id: `1RQu-KZd3DogXGexGTJD58AV-Cir5K6tS`; אם נכשל — חפש `_QA_CACHE_v2` בערוץ `00_SITE_PROTOCOL` (תיקייה `1_Xm87aoiaPEaJZ_CJGFi7wTQffgc3CUz`) |

**מקור מורחב — `R10_QA_KNOWLEDGE.html`** (תוצר כריית 166 הסשנים, ‎204 רשומות):
בתוך ה-HTML מוטמע מערך `const DATA = [...]` עם סכמה: `id, date, question, answer,
sources, tags, related, topic, repeat_count, value`. אם ה-JSON לא נתן התאמה —
חפש גם שם. Drive file-id: `1FZfzBwcHnTX9IoCughXq1IQr5Yhquvm4`; מקומית — חפש
`R10_QA_KNOWLEDGE.html` תחת עץ `ROTHSCHILD_10_CORE` (G: או מירור D:).
(עד שהמבצע ימזג את שני המקורות ל-JSON אחד — בדוק בשניהם.)

אם אף נתיב לא זמין — אמור זאת מפורשות (אל תבטיח חיבור שאין) ועבור ישירות לשלב 3ג.

## שלב 2 · חיפוש במטמון

סכמת רשומה: `id, question, answer, keywords, tags, reasoning_chain, search_path,
partial_hints, concepts, related_qa_hints, source_files`.

1. התאם את השאלה מול `question` + `keywords` + `concepts` + `tags`.
   עברית: התעלם מהבדלי כתיב מלא/חסר ומילות-יחס.
2. אסוף גם רשומות-שכנות דרך `related_qa_hints`.

## שלב 3 · מענה לפי רמת-התאמה

- **(א) התאמה מלאה** → ענה ישירות מ-`answer`. צטט `id` + `source_files`. סיימת.
- **(ב) התאמה חלקית** → פתח מ-`partial_hints` + `reasoning_chain`, והשלם קריאה
  ממוקדת לפי `search_path` — רק הקבצים הרלוונטיים, לא סריקת-תיק מלאה.
- **(ג) אין התאמה** → חיפוש מלא בתיק לפי סדר: תיק-מתקן מעובד →
  `00_SITE_PROTOCOL` → תדריכי-סיור (GUARD_TOUR_TECH_BRIEFING) → תמלולים.
  לאחר שענית — שלב 4.

## שלב 4 · למידה — כל תשובה חדשה חוזרת למטמון

- **מקומי (מבצע):** הוסף רשומה חדשה (`QAnnn` עוקב) באותה סכמה בדיוק — דרך
  `qa_cache.py` אם קיים, אחרת עריכה ישירה של הקובץ.
- **ענן (יועץ):** Drive MCP יוצר בלבד, לא מעדכן — צור
  `_QA_APPEND__MSG_YYYYMMDD-HHMM.json` בערוץ `00_SITE_PROTOCOL` עם הרשומות
  החדשות; המבצע ימזג בקצה שלו.

## פורמט תשובה לעודד

1. **התשובה עצמה בשורה הראשונה, מודגשת.** (לקח Sovilco: "מנטרלים התקן" — לא שאלת-נגד.)
2. מקור: `QAxxx` / קובץ-מקור.
3. רמת-ביטחון רק אם יש ספק. אם המטמון סותר את תיק-המתקן — **התיק גובר**,
   וסמן את הרשומה לתיקון (שלב 4).
